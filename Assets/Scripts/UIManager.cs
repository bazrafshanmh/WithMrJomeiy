using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UIManager : MonoBehaviour
{
    private static string _state = GameManager.GetState();
    void Update()
    {
        if (_state == "Loose")
        {
            
        }
    }
}
